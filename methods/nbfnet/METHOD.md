# nbfnet

- **Kind:** trained model
- **Campaign:** `biosnap-dti-v2` (BioSNAP DTI, split seed 41)
- **Test set:** sealed
- **Loss:** to be specified before training
- **Train in this repo:** yes if hours remain
- **Status:** deferred
- **Notes:** All-candidate path method. One seed. Not SEAL.

Runs belong in `runs/biosnap-dti-v2/nbfnet/<seed>/`.
Update `docs/STATUS.md` in the same change that flips status to `done`.
